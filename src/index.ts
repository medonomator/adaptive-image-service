const port = Number(process.env.PORT ?? 3000);

console.log(`adaptive-image-service ready on port ${port}`);

// TODO: See Tasks in README.md and add the healthcheck endpoint.
// TODO: See Tasks in README.md and implement the core POST/GET handler using stdlib only.
// TODO: See Tasks in README.md and add request validation plus error handling.